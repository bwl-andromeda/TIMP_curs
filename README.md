# Курсовая работа по "Технологиям и методам программирования"

Для установки зависимостей создайте виртуальное окружение:
```zsh
python -m venv env
```
После установите зависимости с помощью:
```zsh
pip install -r requirements.txt
```